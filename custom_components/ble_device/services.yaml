write_value:
  description: "Write a value to BLE characteristic"
  fields:
    value:
      description: "Value to write (int, string, or hex bytes)"
      example: "Hello"
    format:
      description: "Format of the value: int, string, hex"
      example: "string"
